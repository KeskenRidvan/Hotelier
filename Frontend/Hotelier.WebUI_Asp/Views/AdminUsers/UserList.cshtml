@model List<AppUserGetDto>

@{
    ViewData["Title"] = "UserList";
    Layout = "~/Views/AdminLayout/_AdminLayout.cshtml";
}

<div class="col-lg-12">
    <div class="active-member">
        <div class="table-responsive">
            <table class="table table-xs mb-0">
                <thead>
                    <tr>
                        <th>Kullanici</th>
                        <th>Sehir</th>
                        <th>Ulke</th>
                        <th>Durum</th>
                        <th>Cinsiyet</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td><img src="@item.ImageUrl" class=" rounded-circle mr-3" alt="">@item.Name @item.Surname</td>
                            <td>@item.City</td>
                            <td>
                                <span>@item.Country</span>
                            </td>
                            @if (item.Gender is "Erkek")
                            {
                                <td>
                                    <div>
                                        <div class="progress" style="height: 6px">
                                            <div class="progress-bar bg-success" style="width: 90%"></div>
                                        </div>
                                    </div>
                                </td>
                            }
                            @if (item.Gender is "Kadin")
                            {
                                <td>
                                    <div>
                                        <div class="progress" style="height: 6px">
                                            <div class="progress-bar bg-warning" style="width: 90%"></div>
                                        </div>
                                    </div>
                                </td>
                            }

                            @if (item.Gender == "Erkek")
                            {
                                <td><i class="fa fa-circle-o text-success  mr-2"></i> Erkek</td>
                            }
                            @if (item.Gender == "Kadin")
                            {
                                <td><i class="fa fa-circle-o text-warning  mr-2"></i> Kadin</td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<br />
<a asp-controller="Register" asp-action="Index" class="btn btn-primary">Yeni Kullanici Kaydi</a>
